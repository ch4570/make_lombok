package com.rex.annotation;

public enum AccessLevel {

    /*
    *   기본 생성자 생성시 접근 제어 수준을 지정할 수 있는 열거 타입
    *   PUBLIC -> public
    *   PROTECTED -> protected
    *   PACKAGE -> default
    *   PRIVATE -> private
    * */
    PUBLIC, PROTECTED, PRIVATE, PACKAGE
}
